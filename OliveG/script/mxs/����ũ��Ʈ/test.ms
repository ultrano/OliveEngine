kTestSelect = selection as array;
kTestSelect = kTestSelect[1]



-- showproperties kTestSelect.transform.controller
 kTestSelect.rotation.controller.z_rotation
 
 -- bipObj = biped.createNew 100 100 [0,0,0]

-- bip = bipObj.controller

-- b=bezier_position()

-- biped.createPosSubAnims bip b true

-- biped.setPosSubAnim bip [20,0,60] $'Bip01 R Hand' true
